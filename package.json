{
  "name": "patient-tracker",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@capacitor/cli": "^7.4.2",
    "@capacitor/core": "^7.4.2",
    "autoprefixer": "^10.4.21",
    "firebase": "^10.12.3",
    "html5-qrcode": "^2.3.8",
    "postcss": "^8.4.6",
    "postcss-load-config": "^6.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1",
    "tailwindcss": "^4.1.11",
    "web-vitals": "^5.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "capacitor:build": "npm run build && npx cap sync",
    "capacitor:android": "npm run build && npx cap add android && npx cap sync android",
    "capacitor:ios": "npm run build && npx cap add ios && npx cap sync ios",
    "capacitor:open:android": "npx cap open android",
    "capacitor:open:ios": "npx cap open ios"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
